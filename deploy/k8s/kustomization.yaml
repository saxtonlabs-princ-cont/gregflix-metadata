apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - service.yaml
  - deployment.yaml
  - migration-job.yaml

images:
  - name: registry.home.arpa/gregflix-metadata
    newTag: latest

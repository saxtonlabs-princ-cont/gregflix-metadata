apiVersion: v1
kind: Secret
metadata:
  name: gregflix-metadata-secrets
  namespace: gregflix
type: Opaque
stringData:
  GREGFLIX_POSTGRES_PASSWORD: replace-me
  TMDB_READ_ACCESS_TOKEN: replace-me
  # Optional legacy v3 API key. Required only when providers.tmdb.auth_mode is api_key.
  TMDB_API_KEY: replace-me
